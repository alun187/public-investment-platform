server:
  port: 8130 # 服务端口

spring:
  profiles:
    active: dev # 环境设置
  application:
    name: service-oss # 服务名

#阿里云短信
aliyun:
  oss:
    key-id: key-id
    key-secret: secret
    endpoint: endpoint
    bucketName: bucketName